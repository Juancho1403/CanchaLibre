/**
 * Reexport del NavBar global para no duplicar barras fijas ni estilos legacy.
 * Los imports existentes desde `./NavBar` o `Socio/NavBar` siguen funcionando.
 */
export { NavBar } from "../NavBar";
